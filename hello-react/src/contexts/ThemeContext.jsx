// src/contexts/ThemeContext.jsx
import { createContext } from 'react';

// Create the context. We can pass a default value, but we'll provide it in App.jsx.
export const ThemeContext = createContext();